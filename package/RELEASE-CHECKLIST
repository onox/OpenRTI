* Builds everywhere?
* Tests have passed?
  (can use script in package/src/compile-test)
  
* Make sure the protocol version is non beta
* Create a release branch
* Complete the CHANGES file
* Update the packaging version numbers
  Top level cmake file
  rpm spec file
* Add a release tag

* Update the CHANGES file in master

